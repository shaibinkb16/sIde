import React from 'react';

const LivePreview = () => {
  return (
    <div className="live-preview">
      <h3>Live Preview</h3>
      {/* Live preview of HTML/CSS/JS changes goes here */}
    </div>
  );
};

export default LivePreview;
